{
  "name": "backend",
  "private": true,
  "version": "0.0.0",
  "description": "MemMemory backend",
  "scripts": {
    "build": "rimraf dist && tsc",
    "dev": "ts-node src/index.ts",
    "start": "node dist/index.js",
    "prettify": "prettier --log-level warn --cache --write '{src/**/*,*}.{ts,tsx,js,json,yml,scss}' '!**/{node_modules,dist}/**/*'"
  },
  "devDependencies": {
    "@types/node": "^25.8.0",
    "prettier": "^3.8.3",
    "rimraf": "^6.1.3",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  }
}
