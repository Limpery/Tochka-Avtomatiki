generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
}

model Mem {
    id          String @id @default(uuid())
    name        String @unique
    description String
    text        String
}
